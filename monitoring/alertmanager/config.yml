global:
  resolve_timeout: 5m

route:
  receiver: 'slack-notifications'
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: 'slack-notifications'
    webhook_configs:
      - url: 'https://hooks.slack.com/services/REPLACE/ME'
